---
title: Get Session
description: Fetch one session by id, with its status, step count, live view URL
  and structured output as they stand right now.
full: true
_openapi:
  preload:
    - ./data/openapi.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: "GET /v3/sessions/{session_id}"
      - content: "Get Session"
      - content: "Fetch one session by id, with its status, step count, live view URL and structured output as they stand right now."
      - content: "Parameters: session_id, x-browser-use-api-key"
      - content: "Response fields: id, status, model, title, output, outputSchema, stepCount, lastStepSummary, isTaskSuccessful, liveUrl, recordingUrls, profileId, workspaceId, proxyCountryCode, maxCostUsd, totalInputTokens, totalOutputTokens, llmCostUsd, proxyCostUsd, browserCostUsd, proxyUsedMb, totalCostUsd, screenshotUrl, reasoningEffort, failureKind, failureStatusCode, agentmailEmail, integrationsUsed, createdAt, updatedAt"
      - content: "Status codes: 200, 401, 404, 422, 429"
      - content: Fetch one session by id, with its status, step count, live view URL and
          structured output as they stand right now.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Get Session. Send this to your own instance, replacing `your-host` with the hostname it answers on.

```bash
curl -X GET "https://your-host/v3/sessions/{session_id}" \
  -H "Authorization: Bearer $OPENBROWSE_API_KEY"
```

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./data/openapi.json" operations={[{"path":"/v3/sessions/{session_id}","method":"get"}]} />
    </>
  );
}